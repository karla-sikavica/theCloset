import { useCurrentUser } from "../hooks/useCurrentUser";

const Profile = () => {
  /*   const user = useCurrentUser();
   */ return (
    <div>
      <h2>Profile New Item</h2>
      {/* Form or upload logic goes here */}
    </div>
  );
};

export default Profile;
